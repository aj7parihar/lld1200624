package in.aj7parihar.lldoops2200624.lld1class5.interfaces;

public interface Bird extends Animal {
    // When a class is derived from interface we use "implements" keyword
    // But when an interface is derived from interface we use "extends" keyword

    void fly();
}
